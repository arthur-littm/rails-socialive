<script src="https://checkout.stripe.com/checkout.js" id="pay-button" class="stripe-button"
         data-key="<%= Rails.configuration.stripe[:publishable_key] %>"
         data-name="Rubies"
          data-email="<%= current_user.email %>"
          data-description="<%= rubies %> Rubies"
          data-amount="<%= amount %>"
          data-currency="gbp">
</script>
